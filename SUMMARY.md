# AI generated summary: Wed Aug 19 01:10:43 UTC 2026

This summary was generated by DeepSeek using model deepseek-v4-flash. LLMs can make mistakes so treat this as a *guide* not as a *rule-book*

Report written to `benchmark-report.md`. Here's the executive summary:

## Key Findings

**Scope**: 55 Criterion benchmark groups × 16 map implementations (all `target/criterion/` data, median point estimates).

**Fastest per family**:

| Family | Winner | Detail |
|---|---|---|
| Serial default-hasher (create, insert, 9 workloads) | **rustc-hash** | 11–42% ahead of next best everywhere |
| u64 lookups (same-hasher) | **hashbrown** | ahash/std within 1–3% |
| String lookups & same-hasher insert | **ahash** | hashbrown/std within 1–9% |
| Iteration | **indexmap** | 3–4× faster (contiguous storage) |
| Removal | **horde** | ~1.2–1.4× faster at all sizes |
| Clone | **immutable-chunkmap** | ~4 ns at any size (O(1) structural sharing) |
| Concurrent, 2 threads | **txmap** | wins all 9 workloads |
| Concurrent, 4 threads | **leapfrog** | most workloads (⚠ noisy) |

**Ranking**: rustc-hash (13 wins), txmap (10), leapfrog (7), indexmap (6), ahash (5), hashbrown (5), horde (3), immutable-chunkmap (3), std (2), dashmap (1). std is the most consistently "close but not first" (9 second-place finishes within 10%).

**Critical caveats**:
- **4-thread concurrent results are very noisy** — rel std-dev up to 93% (papaya read-heavy 100k), and dashmap's balanced-100k win has median 939 µs vs mean 1,329 µs. Leapfrog/txmap/dashmap ordering at 4 threads is provisional.
- **Papaya lookup-miss anomaly**: 4.85 µs at map-size 1k vs 0.96 µs at 10k (5×, consistently slow) — needs investigation.
- Several measurements sit near the harness floor (~2.3 ns/op lookups, ~4 ns clone).

**Limitations found in wrappers**: rustc-hash/btreemap/concread/immutable-chunkmap lack custom-hasher support (excluded from same-hasher groups); leapfrog needs `K: Copy` (no String keys) and has no Clone; flurry requires `K: Ord`; concread needs `Debug + 'static` and is 10–100× slower than alternatives (COW commit per write); starshard requires `V: Clone` even for insert; horde needs `&mut` for writes (no concurrent bench); indexmap's remove uses `swap_remove` (order not preserved).

**Recommendations**: re-measure 4-thread concurrency with longer runs, investigate the papaya anomaly, add out-of-the-box lookup/remove benchmarks (rustc-hash can't join same-hasher groups), and consider dropping flurry/concread which contribute no actionable signal.
